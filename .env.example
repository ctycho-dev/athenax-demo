# SMTP Configuration for Server-Side Email
# These are server-side only (NOT NEXT_PUBLIC_) for security

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Sender Information
SMTP_FROM=noreply@athenax.com
SMTP_FROM_NAME=AthenaX Incubator

# Admin Notification Email
ADMIN_EMAIL=admin@athenax.com

# Gmail Setup Instructions:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Create an app password for "Mail"
# 5. Use that app password as SMTP_PASS above
#
# For other SMTP providers (SendGrid, Office365, etc.):
# Update SMTP_HOST, SMTP_PORT, SMTP_USER, and SMTP_PASS accordingly
# See documentation for provider-specific settings